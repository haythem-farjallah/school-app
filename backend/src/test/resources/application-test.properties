# Datasource and Redis connections come from Testcontainers (@ServiceConnection).
jwt.secret=test-only-jwt-signing-key-not-used-anywhere-else-0123456789

# No SMTP server in tests; mail sending fails and is logged.
spring.mail.host=localhost
spring.mail.port=1025
spring.mail.username=no-reply@school.test
spring.mail.properties.mail.smtp.auth=false
spring.mail.properties.mail.smtp.starttls.enable=false
