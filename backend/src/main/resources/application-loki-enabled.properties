# Opt-in log shipping to Loki (see logback-spring.xml).
# Activate together with another profile, e.g. SPRING_PROFILES_ACTIVE=dev,loki-enabled.
# Override the endpoint with LOKI_URL.
loki.url=http://localhost:3100/loki/api/v1/push
